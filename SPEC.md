# SPEC.md — kiban-lens(基盤レンズ)

<!-- scaffold template v1.8.0 から展開(2026-08-20)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

AI 基盤モデルを提供する主要企業(米国 6・中国 7・日本 10 の計 23 社)の公式ブログ / ニュースの最新 5 件のヘッドラインを、Claude(Haiku)による和訳付きで一覧できる静的ページを提供する。収集 → 和訳 → 生成 → 配信を GitHub Actions の 6 時間 cron で自動化する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | `src/sources.py` に 23 社の取得経路(feed / html / pending)を宣言的に保持する。html は一覧からのリンク採取+記事ページからの日付・タイトル抽出の統一方式 | must |
| F-02 | `src/fetch.py` が各社最新 5 件を収集する。取得失敗・pending は前回分を保持(劣化継続)し、全社失敗のみ exit 1 | must |
| F-03 | 英語・中国語の見出しは Claude API(claude-haiku-4-5・構造化出力)で和訳し、`data/translations.json` に永続キャッシュする(新出のみ翻訳)。API キー未設定・障害時は原文のまま表示 | must |
| F-04 | `src/render.py` が地域別 3 セクション・和訳+原文併記・最終更新(JST)・取得成功数を含む自己完結の `out/index.html` を生成する(決定論) | must |
| F-05 | フッタはフリート規約(MIT License © ・ GitHub ・ App Menu)。歩き方/設計図アーティファクトは後続ループで追加 | should |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | テストは全てオフライン(ネットワーク・API 呼び出しなし) | pytest がネットワーク不要で緑 |
| N-02 | レンダリングは決定的(同じ入力から同じ出力) | T-05 |
| N-03 | 翻訳コストは新出見出し分のみ(キャッシュ命中時はゼロ) | T-03 |
| N-04 | 記事ページ連続取得に 0.5 秒間隔の礼儀 | fetch.http_get |

## 4. 品質基準

- 稼働 18 社(2026-08-20 実測)。pending 5 社(Moonshot・Zhipu・ByteDance・Tencent・rinna)はカードに「取得経路調査中」と理由を表示
- 劣化継続: 取得失敗時は前回分+「最新取得に失敗」注記。翻訳失敗時は原文表示
- 見出しの和訳には必ず原文を併記する(誤訳の検証可能性)

## 5. スコープ外

- JS レンダリング必須サイトのヘッドレスブラウザ取得(pending 5 社の解消は後続ループで headless 化 or 代替経路調査)
- 記事本文の要約・翻訳(見出しのみ)
- 見出しの検索・絞り込み UI(初版は一覧のみ。需要があれば後続ループ)
